# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# pnpm 的本地包缓存（内容寻址存储，pnpm 会自动重建，绝不能入库）
.pnpm-store

# Rust 构建产物（src-tauri/target）
target
# Tauri 由 tauri-build 生成的 ACL/能力 schema，构建时自动重建
src-tauri/gen/schemas

# 由 scripts/copy-decoders.mjs 从 node_modules/three 复制生成，不入库
public/draco
public/basis

# 性能回归用的生成物（scripts/make-benchmark-model.mjs 产出，MB 级）
benchmark-models

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
